name: 'Check PR title'

on:
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize

jobs:
  main:
    name: Validate PR title
    runs-on: ubuntu-slim
    permissions:
      pull-requests: read
    steps:
      - uses: health-informatics-uon/workflows/.github/workflows/pr-title.yml@main
        secrets: inherit
